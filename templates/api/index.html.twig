{% extends 'base.html.twig' %}

{% block title %} API GitHub{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1> Liste des repositories GitHub de Blanchard ✅</h1>
        
    <ul>
        {% for repo in reposGit %}
            ID : {{ repo.id }} <br>
            Nom complet : {{ repo.full_name }} <br>
            Nom du repos : <a href="{{ path('app_api_show', {id: repo.id }) }}" target='_blank'> {{ repo.name }}  </a>  <br>
            Description : {{ repo.description ?? "Pas description" }} <hr>
        {% endfor %}
    </ul>
</div>
{% endblock %}
